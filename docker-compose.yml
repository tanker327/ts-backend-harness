services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

  redis-test:
    image: redis:7-alpine
    ports:
      - "6380:6379"

volumes:
  redis-data:
